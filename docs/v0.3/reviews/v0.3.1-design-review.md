# v0.3.1 Design Review (running doc)

> Owner: Greg + Orrery. Started 2026-07-14. Living doc — updated as the
> review steps through questions. Scope: design-level, whole-game. Code
> architecture lives in the 2026-07-04 deep review.

## Greg's Frame (2026-07-14)

Ideal outcomes and problem areas, verbatim intent:

1. **Systems are scaffolded, not fulfilled.** Individual systems are
   implemented but don't fulfill design intent. Each likely needs an explicit
   **v2 goal** to iterate on and measure success against.
2. **Examples:** NPCs/AI feel like stubs, not features. Slingshot isn't
   usable or well telegraphed — should feel like a grappling hook.
   Inhibitors read as tapestry, not threat. Plenty more.
3. **Design docs are stale.** Originals need updating, archiving, or
   versioning. What was the original goal? Did we do it? Sometimes yes,
   sometimes no. Old docs are **not sacrosanct**.
4. **Tunables and math are loose.** Hard to tell if player-usable skill does
   anything against the larger sim physics. Applies to anything with a
   number attached. (See Red Flags Ledger.)

The five tensions below remain valid inputs, but the pass is broader: audit
every system against intent, set v2 goals, and version the doc truth.

## Question Queue

Stepped one at a time with Greg. Status: ⬜ open · ✅ answered.

- ✅ **Q1 — The bar.** GREG: every system reaches at least v2 in the next
  minor version. Breadth is the bar; no system ships v0.3.1 still at
  scaffold quality.
- ✅ **Q2 — System inventory.** GREG: split fauna / hostile AI / fake
  players (AI crew) into their own designs; VFX + UI/UX feedback is its own
  item. Table is now 18 systems.
- ✅ **Q3 — v2 goal template.** GREG: shape confirmed with edits — see S3.
- ✅ **Q4 — Slingshot (worked example).** GREG: approved with additions —
  staggered starting biases for conflicting tunables, and the units
  doctrine + ruler-layer debug design (see S4).
- ⬜ **Q5+ — Per-system passes continue.** For each remaining system:
  original intent vs shipped truth, v2 goal per S3 template, tunables with
  bias, ruler-layer handlers.
- ⬜ **Qn — Docs policy.** Archive-and-rewrite vs version-in-place for the
  old design docs; who owns the delta audit.
- ⬜ **Qn — Tunables policy.** Perceivable-units rule, skill-effect floor
  (player input must move outcomes by a felt margin), config audit scope.

## System Audit Table

Status: 🔲 to go · 🔎 in review · ✅ v2 goal set.

| System | Original intent (short) | Current read | Status |
|---|---|---|---|
| Movement/thrust | Surfing spacetime is the game | Feel verdict pending; assists deferred | 🔲 |
| Waves/surfing | Read the flow, ride it | Waves exist; not taught, not telegraphed | 🔲 |
| Slingshot | Grappling hook around wells | v2 goal set (S4) | ✅ |
| Wells/collapse | Universe is the clock | Sim-real, visually mute | 🔲 |
| Signal | Consequence, taxes ambition | Meter, not ecology | 🔲 |
| Ambient fauna | World feels alive, fabric-coupled | Stubs; presence without behavior | 🔲 |
| Hostile AI | Threats you respect and read | Stubs; aggro exists, menace doesn't | 🔲 |
| Fake players (AI crew) | Solo = 1 human + AI crewmates; MP-first design | Design not started as its own system | 🔲 |
| Inhibitor | Dread, inevitable, your fault | Tapestry, not threat | 🔲 |
| Wrecks/salvage | Terrain + stakes | Loot nodes; aging/cluster ideas unshipped | 🔲 |
| Portals/extraction | Destinations that shape routes | Confirm-flow shipped; route-shaping thin | 🔲 |
| Hulls/rigs/progression | Purchase changes next run | Rig tracks live; legibility unproven | 🔲 |
| Economy/EM ledger | Truthful stakes | Ledger honesty fixed; death economy tuned? | 🔲 |
| Chronicle/meta | Reason to run it twice | Career strip + 5 runs; echoes curtained | 🔲 |
| Routes/maps | Distinct identities, teaching route | 3 maps live; Shallows teaching intentional? | 🔲 |
| Audio | Register + dread carrier | Event audio live; dramatic arc unused | 🔲 |
| HUD/UI | Supplement, never replace world | Pressure meter carries the clock | 🔲 |
| VFX / UI-UX feedback | Every action telegraphed and answered | Smeared across systems; telegraphing falls through cracks | 🔲 |

## Inputs: The Five Tensions (Orrery, 2026-07-14)

Greg agrees with 1–4; 5 expanded.

1. **RC gate measures the machine, not the game.** Clock and Dread pillars
   have no expression surface and no evidence gate. Build likely fails
   "mute the HUD — can you feel the urgency?"
2. **Signal is a meter, not an ecology.** Consequence chain (loud act →
   heard → coming) is not legible. Wiring, not new systems.
3. **Inhibitor is a mechanic, not a presence.** Arrival exists; anticipation
   and aftermath don't. Magenta is reserved but nothing escalates in it.
4. **Mastery has nowhere to compound.** Nothing teaches/rewards reading the
   fluid; improvement is loot RNG and route memory.
5. **Pressure meter: debug readout became product truth.** Inhibitor
   pressure (signal + time + well growth) is the convergence of pillars 3/4/5
   and renders as `building // 42%`. Precision kills dread; players will
   optimize the meter. Root cause: no dramatic-data channel to the fabric,
   so every surface (solo HUD, v0.4 crew rail) taps UI. Direction: banded
   fabric tells (palette drain, glyph decay, drone detune), form transitions
   as world events, extend the existing corruption grammar into the fabric,
   demote the meter. Falsifiable gate: order HUD-less screenshots at
   pressure 0.2/0.5/0.9.

## S4 — Slingshot v2 Goal (locked) + Debug Handler Design

### Slingshot v2 goal

**Feel target.** Thematic: my ship can dive at a gravity well, commit to the
arc, and steal momentum from the dying star — leaving faster than I arrived,
because physics still works at the end of the universe. Reference:
Spider-Man's swing — attach is generous and clearly shown, the game owns the
arc while I own the timing, release direction is my skill expression under
rules learned in one encounter. (Reference-research step: also pull Just
Cause grapple and Outer Wilds gravity assist before locking.)

**Tunables (5), with starting bias.** Conflicting assists get staggered
starts so effects are attributable one at a time. Some may sit at 0/disabled
— the lever exists even if unused.

| Tunable | Unit | Start bias |
|---|---|---|
| `captureRadius` | meters, drawn on screen | medium |
| `magnetism` | max entry-bend, degrees | **large** — overshoot, dial back |
| `coyoteTime` | ms | **small** |
| `payoffCurve` | exit speed ÷ entry speed per quarter-turn | medium |
| `chainWindow` | seconds | may start 0/disabled |

**Test.** Fixture map: agent using only these five knobs makes a slingshot
route beat thrust-only by a target margin; capture shows the telegraph chain
(aim cue → lock → arc → release ghost); a first-time player can state the
rules after three swings.

### Units doctrine (GREG, applies to every system)

Tuning values must converge on human-readable, **screen-readable** units — a
value translates to x meters on screen.

- Canonical spatial unit: the **meter**, pegged in fiction to hull length
  (e.g. Drifter = 12 m — exact peg is a Greg call). No normalized 0–1
  spatial fractions in tunable contracts.
- One conversion pipeline: sim units → meters → screen px, in a single
  module beside `coords.js` (same no-inline-conversion rule).
- Spatial tunables carry units in name or schema: `captureRadius_m`.

### Debug handler design — "the ruler layer"

Toggleable dev overlay rendering every contract tunable on the live game:

- **Scale bar** — always-on ruler showing 100 m at current zoom.
- `captureRadius` — true-scale ring per well, labeled ("450 m").
- `magnetism` — deflection vectors along the approach path; label max bend.
- `coyoteTime` — ring lingers ghosted after exit, fade = window remaining.
- `payoffCurve` — entry vs projected exit vector pair at release point.
- `chainWindow` — countdown arc toward the next eligible well.

**Handler contract (the enforcement):** every tunable in a system contract
MUST register a draw handler — spatial → true-scale geometry + meter label;
temporal → visible window/decay; multiplier → before/after vector pair. A
tunable that cannot be drawn cannot be felt and fails review. This is the
0.000x red flag turned into policy. Live readout panel: value in human
units beside each slider; slider moves → geometry moves same frame.

## Red Flags Ledger

- **0.000x tuning constants** (Greg): e.g. `pressureFromTime: 0.0005/s`.
  Wrong units, hidden normalization, or an unfeelable knob. Audit all
  sub-0.01 sim constants; re-express in perceivable units (time-to-threshold,
  per-run totals).
- **Skill-vs-physics legibility** (Greg): player-usable skill effects are
  indistinguishable from ambient sim forces. Candidate rule: any player
  ability must move an observable outcome by a felt margin, provable by an
  A/B probe.

## Solutions Log

Decisions land here as questions resolve.

- **S1 (from Q1):** v0.3.1 scope = all systems to at least v2. Every entry in
  the audit table gets an original-intent check, a v2 goal, and a
  measurement before the release closes. Sequencing within the release can
  still prioritize the run-critical loop, but nothing is exempt.
- **S2 (from Q2):** inventory locked at 18 systems. Fauna, hostile AI, and
  fake players (AI crew) are separate designs with separate v2 bars — fauna
  = alive world, hostiles = respected threat, fake players = credible
  crewmate in the MP-first solo mode. VFX/UI-UX feedback is a first-class
  system so telegraphing has an owner.
- **S3 (from Q3): the v2 goal template.** Per system, in this order:
  1. **Feel target — two sentences.** (a) Thematic intent inside the game's
     fiction ("my ship can swing around a star to gain thrust because
     physics"). (b) A specific reference-game example with a clear rule set
     ("in Spider-Man I web-swing building to building and the game handles
     it within clear rules"). Greg's examples are illustrative, not
     binding — each system pass includes a short reference-research step to
     find the right comparison.
  2. **Tunables — minimal set, named, perceivable units.** As few as
     necessary (e.g. grapple distance, magnetism, coyote time). Tunables
     come first; the contract is built on them.
  3. **Test — goal-vs-tunables.** Can the feature hit the feel target using
     only the named tunables? If hitting the goal requires touching numbers
     outside the contract, the contract is wrong.

## Outstanding / Examined

- **Examined:** pressure meter (tension 5) — direction set, awaiting v2 goal
  formalization in per-system pass.
- **To go:** everything in the audit table; docs versioning policy; tunables
  audit scope.
- **Parked:** branch hygiene (`codex/v0.3-orrery-rc-fixes` unmerged 8
  commits) — ops, not design, but blocks a clean v0.3.1 base.

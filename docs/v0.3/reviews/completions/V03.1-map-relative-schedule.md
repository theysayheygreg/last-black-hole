# v0.3.1 Map-Relative Schedule Completion

Date: 2026-07-17

The selected map now supplies the one product run duration through the shared
map-scale registry: Shallows `480s`, Expanse `600s`, and Deep Field `720s`.
ESM/CJS consumers, the match Conductor, authority snapshots, client clock,
results, and presentation/ruler state all use that resolved value. Signature
run-duration overrides were removed; the short `LBH_SIM_MAX_SIM_TIME` path is
explicitly test-only.

Migrated whole-run fronts:

- collapse: `0/.25/.50/.75`
- Inhibitor: `0/.15/.30/.45`
- optional portal targets: `.075 + .20 * index`
- final exfil open: `1.00`

Intentionally absolute clocks are optional portal durations `90/75/60/45/30s`,
the final exfil close duration `60s`, tells, cooldowns, action windows, effect
lifetimes, the `10s` offset guard, and reconnect/reservation timers. On shorter
tiers, the existing guard can resolve a portal opening forward while retaining
its normalized target metadata and absolute duration.

The focused contract proves exact 480/600/720 ESM/CJS parity, all epoch
schedules and vectors, proportional Inhibitor/portal/final-exfil fronts,
Expanse anchor timings, deterministic IDs/order, final close-derived hard end,
the Conductor path, and the accepted short-fixture override. The locked
baseline is complete; later multiplier balance remains playtest-tunable.

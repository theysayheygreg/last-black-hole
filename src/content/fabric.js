// Browser view of the same fabric defaults consumed by authority. Live Bench
// overrides may tune these values, but product defaults have one data owner.
import data from './fabric.data.json' with { type: 'json' };

export const FABRIC = data;

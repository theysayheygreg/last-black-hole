// Browser/local-sandbox view of the same arcade grapple contract used by the
// authoritative CJS runtime. Keep feel constants in the JSON, not in loops.
import data from './grapple-arc.data.json' with { type: 'json' };

export const GRAPPLE_ARC = data;

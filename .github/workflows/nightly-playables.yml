name: Weekly Playables

on:
  schedule:
    - cron: '17 9 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: lbh-weekly-playables
  cancel-in-progress: false

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.changes.outputs.should_build }}
      last_success_sha: ${{ steps.changes.outputs.last_success_sha }}
    steps:
      - name: Decide whether weekly build should run
        id: changes
        uses: actions/github-script@v7
        with:
          script: |
            if (context.eventName === 'workflow_dispatch') {
              core.setOutput('should_build', 'true');
              core.setOutput('last_success_sha', 'manual');
              return;
            }

            const { owner, repo } = context.repo;
            const workflowId = 'nightly-playables.yml';
            const currentSha = context.sha;

            const response = await github.rest.actions.listWorkflowRuns({
              owner,
              repo,
              workflow_id: workflowId,
              status: 'success',
              per_page: 10,
            });

            const priorRun = response.data.workflow_runs.find((run) => run.id !== context.runId);
            if (!priorRun) {
              core.info('No prior successful weekly build found; building.');
              core.setOutput('should_build', 'true');
              core.setOutput('last_success_sha', 'none');
              return;
            }

            core.setOutput('last_success_sha', priorRun.head_sha);
            if (priorRun.head_sha === currentSha) {
              core.info(`Current SHA ${currentSha} matches the last successful weekly build; skipping build jobs.`);
              core.setOutput('should_build', 'false');
              return;
            }

            core.info(`Current SHA ${currentSha} differs from last successful weekly SHA ${priorRun.head_sha}; building.`);
            core.setOutput('should_build', 'true');

  build-web:
    needs: detect-changes
    if: needs.detect-changes.outputs.should_build == 'true'
    env:
      ENABLE_GITHUB_PAGES: ${{ vars.LBH_ENABLE_GITHUB_PAGES == 'true' }}
    runs-on: ubuntu-latest
    outputs:
      release_body: ${{ steps.meta.outputs.release_body }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build web
        run: npm run build:web

      - name: Package release assets
        run: node scripts/ci/package-nightly-assets.cjs

      - name: Upload GitHub Pages artifact
        if: env.ENABLE_GITHUB_PAGES == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/nightly/web

      - name: Zip web artifact
        run: |
          cd dist/nightly
          zip -rq last-singularity-web-nightly.zip web

      - name: Build release metadata
        id: meta
        run: |
          {
            echo 'release_body<<EOF'
            echo 'Weekly playable builds for Last Singularity.'
            echo ''
            if [ "$ENABLE_GITHUB_PAGES" = "true" ]; then
              WEB_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
              echo "- Web (GitHub Pages): ${WEB_URL}"
            fi
            echo '- Web zip: attached zip asset'
            echo '- Linux / Steam Deck: attached zip asset'
            echo '- Windows: attached zip asset'
            echo '- macOS: attached zip asset'
            echo ''
            echo 'Steam Deck one-command install from Desktop Mode / Konsole:'
            echo ''
            echo '```sh'
            echo 'curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/scripts/install-steam-deck.sh | bash'
            echo '```'
            echo ''
            echo 'These automated weekly playtest artifacts only rebuild when the scheduled run sees new commits.'
            echo 'The release tag and asset filenames still use nightly-latest / *-nightly.zip for stable installer URLs.'
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Upload web release asset
        uses: actions/upload-artifact@v4
        with:
          name: last-singularity-web-nightly
          path: dist/nightly/last-singularity-web-nightly.zip

  build-win:
    needs: detect-changes
    if: needs.detect-changes.outputs.should_build == 'true'
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build Windows desktop
        run: npm run build -- --targets=win --mode=release

      - name: Package release assets
        run: node scripts/ci/package-nightly-assets.cjs

      - name: Zip Windows artifact
        shell: pwsh
        run: |
          Compress-Archive -Path "dist/nightly/Last Singularity-win32-x64" -DestinationPath "dist/nightly/last-singularity-win-nightly.zip" -Force

      - name: Upload Windows release asset
        uses: actions/upload-artifact@v4
        with:
          name: last-singularity-win-nightly
          path: dist/nightly/last-singularity-win-nightly.zip

  build-linux:
    needs: detect-changes
    if: needs.detect-changes.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build Linux desktop
        run: npm run build -- --targets=linux --mode=release

      - name: Package release assets
        run: node scripts/ci/package-nightly-assets.cjs

      - name: Zip Linux artifact
        run: |
          cd dist/nightly
          zip -rq last-singularity-linux-nightly.zip "Last Singularity-linux-x64"

      - name: Upload Linux release asset
        uses: actions/upload-artifact@v4
        with:
          name: last-singularity-linux-nightly
          path: dist/nightly/last-singularity-linux-nightly.zip

  build-mac:
    needs: detect-changes
    if: needs.detect-changes.outputs.should_build == 'true'
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build macOS desktop
        run: npm run build -- --targets=mac --mode=release

      - name: Package release assets
        run: node scripts/ci/package-nightly-assets.cjs

      - name: Zip macOS app
        run: |
          cd dist/nightly
          ditto -c -k --sequesterRsrc --keepParent "Last Singularity.app" "last-singularity-mac-nightly.zip"

      - name: Upload macOS release asset
        uses: actions/upload-artifact@v4
        with:
          name: last-singularity-mac-nightly
          path: dist/nightly/last-singularity-mac-nightly.zip

  deploy-pages:
    if: needs.detect-changes.outputs.should_build == 'true' && vars.LBH_ENABLE_GITHUB_PAGES == 'true'
    needs: [detect-changes, build-web]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  publish-release:
    if: needs.detect-changes.outputs.should_build == 'true'
    needs: [detect-changes, build-web, build-win, build-linux, build-mac]
    runs-on: ubuntu-latest
    steps:
      - name: Download web asset
        uses: actions/download-artifact@v4
        with:
          name: last-singularity-web-nightly
          path: release-assets

      - name: Download win asset
        uses: actions/download-artifact@v4
        with:
          name: last-singularity-win-nightly
          path: release-assets

      - name: Download linux asset
        uses: actions/download-artifact@v4
        with:
          name: last-singularity-linux-nightly
          path: release-assets

      - name: Download mac asset
        uses: actions/download-artifact@v4
        with:
          name: last-singularity-mac-nightly
          path: release-assets

      - name: Publish weekly release
        uses: ncipollo/release-action@v1
        with:
          tag: nightly-latest
          name: Weekly Playables
          body: ${{ needs.build-web.outputs.release_body }}
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          prerelease: true
          artifactErrorsFailBuild: true
          artifacts: "release-assets/*.zip"
